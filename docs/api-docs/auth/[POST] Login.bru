meta {
  name: [POST] Login
  type: http
  seq: 2
}

post {
  url: http://localhost:8000/api/v1/login
  body: json
  auth: inherit
}

body:json {
  {
    "email": "john@example.com",
    "password": "SecurePassword123!"
  }
}

settings {
  encodeUrl: true
  timeout: 0
}
